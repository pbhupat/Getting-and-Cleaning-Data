About this R script File with R code "run_analysis.R" perform 5 following steps (in accordance assigned task of course work):

Merging the training and the test sets to create one data set. 
  Reading all the train and test datasets.
  Merging train and test sets to form a main dataset.
Extracting only the measurements on the mean and standard deviation for each measurement Assign column names. 
  Extracting mean and standard deviation for each measurement.
Using descriptive activity names to name the activities in the data set
Appropriately labeling the data set with descriptive variable names
Creating a second, independent tidy data set with the average of each variable for each activity and each subject 
  Average of each variable for each activity and subject. 
  Write the final tidy dataset to a text file.
  
 About variables:
x_train, y_train, x_test, y_test, sub_train and sub_test contain the data from the downloaded files.
x_data, y_data and sub_data merge the previous datasets.
main_data is the dataset that is formed by merging x_data,y_data and sub_data for further analysis.

features,activities contains the correct names for the x_data dataset, which are applied to the column names stored in
